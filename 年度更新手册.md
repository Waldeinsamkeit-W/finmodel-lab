# 年度更新手册

明年年报出来之后，怎么把数据更新进去而不出错。

现状是：**报表数字硬编码在 `data/*.js` 里，同一个数字往往出现在好几个模型中**。
比如苹果 FY2024 营业收入 391,035 同时出现在 3 个模型里。改了一处忘了另一处，
页面不会报错、公式照样算得出数——只是算的是错的。这份手册和两个自检脚本就是为了堵这个洞。

---

## 一、更新前先看清要动多少地方

本地起服务器，打开页面，在控制台里加载盘点脚本：

```javascript
const s=document.createElement('script'); s.src='_inventory.js'; document.head.appendChild(s);
```

然后：

```javascript
updateScope()          // 每家公司有多少个硬编码数字、散在哪几个模型里
inventory('inovance')  // 汇川的每一个数字：模型 / 表 / 单元格 / 年份 / 科目 / 取值
```

当前的规模（2026 年 8 月）：

| 公司 | 硬编码数字 | 涉及模型 | 覆盖年份 |
|---|---|---|---|
| 苹果 | 361 | 8 | FY2022–FY2029 |
| 汇川技术 | 186 | 4 | 2023–2027 |
| 贵州茅台 | 169 | 3 | 2022–2029 |
| 比亚迪 | 85 | 3 | 2022–2026 |
| 腾讯 | 59 | 3 | 2022–2025 |
| 招商银行 | 59 | 3 | 2023–2026 |
| 百丽国际 | 43 | 3 | 交易口径 |
| 特斯拉 | 40 | 2 | 2022–2025 |
| 其余 7 家 | 各 10–21 | 各 1–2 | — |

> 注意：「覆盖年份」里的未来年份是预测列（2025E 之类），它们是**教学假设**，
> 不随年报更新而变——更新时只动历史列。

---

## 二、更新步骤

以「汇川技术 2026 年报发布」为例。

### 第 1 步：确定要动哪些文件

```javascript
inventory('inovance')
```

输出里 `model` 字段会告诉你涉及哪几个文件。当前是 4 个：
`case-inovance-3s.js`、`case-inovance-sotp.js`、`gaps-01-inovance.js`（两个模型）。

### 第 2 步：改数

历史列整体左移一年，最右边补上新年度。三张表的每一个 `G(...)` 都要对着年报原文核。

**只改 `G()`，不要改 `I()` 的参考公式**——公式是结构，结构不随年份变。
如果新年度年报的科目结构变了（比如新增了一个报表行），那才需要动结构。

### 第 3 步：跑六个自检

```javascript
const a=document.createElement('script'); a.src='_verify.js'; document.head.appendChild(a);
const b=document.createElement('script'); b.src='_inventory.js'; document.head.appendChild(b);
// 加载完之后
verifyAll()
crossCheck()
verifyGrading()
verifySource()
verifyFuncs()
verifyScenarios()
```

**`verifyScenarios()` 必须满足**（改模型公式之后必跑）：

- `failed` 为 0。它和 `verifyFuncs` 一样用**手算的独立预期值**：把退出价、实际利润、
  协同规模之类的给定格钉成边界值（0、很低、亏损、刚好等于门槛），再和手算结果比。
  2026-09 外部核验抓到的清算瀑布、补偿上限、协同门槛几处错误，在默认输入下全部「配平」，
  只有拿边界值一试才会露馅。以后改这类模型的公式，先在这里加边界用例。

**`verifyFuncs()` 必须满足**（改 engine.js 之后必跑）：

- `failed` 为 0。这是唯一一个**用独立预期值**的测试：预期按 Microsoft 的函数定义
  手写，不是引擎自己算的。前面几个自检都是拿引擎测引擎，只能证明自洽，
  证明不了正确——2026-09 外部审查用 9 个 Excel 用例抓到 XIRR 不读日期、
  ROUND 负数舍错、IFERROR 接不住 NaN 等 9 处偏差，全部是这么漏掉的。
  以后加函数、改函数，先在 `verifyFuncs` 里加用例再动代码。

**`verifyAll()` 必须满足**：

- `errs` 为 0 —— 没有公式报错
- `dangling` 为 0 —— 没有公式指向空格或越界格（这一类最阴，语法合法、算得出数，只是把 0 当成了真实数据）
- `checks` 为 0 —— 所有「应为 0 / 应为 100%」的校验行都对上了
- `bounds` 为 0 —— 声明了取值范围的格子，参考答案自己落在范围内。
  不为 0 说明范围写错了：学生填对反而会被提示「超出范围」
- `failed` 只剩 `inovance-sotp` —— 两处 0.16 / 0.06 的残差。模型第 38–43 行已按数据精度算出舍入容差
  （四个分部各按 0.01 亿元取整，合计最大 ±2 百万元），这两个差额都在容差内；但分部数据取自二手源，
  是不是舍入要拿年报原文的分部表确认。在那之前把它留在这里当已知项，**不要放宽 `TOL` 把它消掉**

**`crossCheck()` 必须满足**：

- `冲突` 为 0 —— 同一家公司、同一个科目、同一年，在不同模型里取值必须一致
- `仅精度不同` 目前是 4 条（茅台三条、苹果稀释股数一条），是同一个数字取了不同小数位，可接受

**`verifyGrading()` 必须满足**（判定机制自身的回归，改数据或改 grade.js 之后都要跑）：

- `误伤` 为 0 —— 把参考公式原样填进去必须判对。这个数不为 0 是最严重的问题：
  判错正确答案会让学生不再信任批改结果，比放过一个作弊的伤害大得多
- `漏网` 为 0 —— 把参考答案的数值写死填进去必须判错
- `原理盲区` 目前 34 格（0.83%），是**正常的，不是缺陷**。这些格子的正确答案
  与数据无关（百分比合计恒为 1、指数基期恒为 100、`IF` 布尔标记、恒等式恒为 0），
  正确公式本身就是常数函数，行为上无法与伪装成公式的常数区分。
  它们仍受结构检查保护：直接敲 `1` 会被判错，只有刻意写成 `=0*B3+1` 才能绕过。
  用 `verifyGrading().blind` 看具体是哪些格。新增模型后这个数变大是正常的，
  但如果某个模型的盲区格特别多，值得回头看看那些格子是不是出得太没信息量

**`verifySource()` 必须满足**：

- `口径可推出` 为 71/71 —— 每个模型的币种和量级都能从 `sheet.unit` 推出来。
  推不出来说明某张表的 `unit` 写成了新花样，回去把它写成和别人一致的格式，
  否则学生页面上那条数据口径就是空的
- `多币种模型` 里出现的模型要单独看一眼：一个模型里混了两种货币，
  除非确实是跨境交易，否则多半是某张表的 `unit` 写错了
- `数据类型.构造` 为 25、`溯源进度.需溯源` 为 46 —— 构造数据由模型声明 `dataKind: 'synthetic'`，
  不再按说明文字猜。新增一个纯教学模型时记得声明，否则它会被算进「需溯源」
- `分类待复核` 为空 —— 不为空说明某个模型的说明文字和 `dataKind` 声明对不上，要人看一眼
- `缺口统计` 是待办清单，不是错误。它列的是只有人能填的东西
  （原文链接、页码、数据截止日、模型版本、最近更新日、尚未核对的部分）。
  更新完数据后，至少把改动过的模型的 `source.updated` 和 `source.version` 填上；
  改了某个数又没对照原文，要把它写进 `source.unverified`，不能让模型继续挂着「已核对」

当前基线（更新前的干净状态）：

```
verifyAll()       → 71 个模型 / 4,104 个作答格 / 70 个零问题 / 1 个已知残差
crossCheck()      → 120 组共用数字 / 冲突 0 / 仅精度不同 4
verifyGrading()   → 误伤 0 / 漏网 0 / 原理盲区 34（0.83%）
verifySource()    → 口径可推出 71/71 / 多币种 0 / 构造 25、需溯源 46：数值已核对 10、部分核对 10、未标注 26
verifyFuncs()     → 35 条独立预期 / 失败 0
verifyScenarios() → 34 条边界情景 / 失败 0
```

### 第 4 步：核对结论文字

这是最容易漏的一步。每个模型的 `takeaways` 和 `intro` 里都写了具体数字
（"三年收入 +48.3%，归母净利润 +6.5%"）。数据一变，这些话就过时了。

`verifyAll()` **查不出这类问题**——它只管公式，不管文字。
只能手工过一遍改动过的模型的 `intro` / `takeaways` / `steps` / `dataNote`。

### 第 5 步：重新打包并部署

```bash
python3 build.py
```

然后按 `部署说明.md` 推上去。

---

## 三、能不能做成全自动

「抓数 → 校验配平 → 生成代码」全自动，理论上可以，但有两个卡点，值得说清楚：

**卡点一：数据源。** A 股年报 PDF 的表格结构每家、每年都不一样，
自动抽取的准确率不足以直接进教学材料。付费数据接口（Wind、Choice）能解决，
但它们的科目口径是二次加工过的，和年报原文不完全一致——
而这套教材的卖点恰恰是"起点是年报原文"。用了接口就得放弃这一点。

**卡点二：倒挤项和教学假设。** 模型里大量科目是倒挤出来的
（"其他经营净额"、"其他流动资产"），它们的定义依赖于这个模型选了哪几个科目单独列示。
新年度的科目一变，倒挤的口径就要重新判断。这一步是设计决策，不是数据搬运。

**所以现实的目标不是全自动，是"改得快、错得少"**：
`inventory()` 告诉你要改哪里，`verifyAll()` + `crossCheck()` 保证你没改错。
剩下的判断留给人。

如果以后要往自动化推进一步，投入产出比最高的是把 `G()` 里的数字抽到一个
带来源标注的 `data/_facts.json`（公司 / 年度 / 科目 / 取值 / 出处页码），
模型改成引用 fact id。那样更新就只改一个文件，`crossCheck()` 也可以退休。
这是个几百行的重构，值不值得做取决于这套教材要维护几年。

---

## 四、两个脚本的分工

| 脚本 | 查什么 | 查不出什么 |
|---|---|---|
| `_verify.js` | 公式报错、校验行不为 0、NaN、公式指向空格；边界情景下的手算预期（`verifyScenarios`）；溯源状态与字段分类（`verifySource`） | 数字本身是不是真实的；结论文字是否过时 |
| `_inventory.js` | 同一数字在多个模型里是否一致；每家公司要改多少处 | 数字是否与年报原文相符 |

两个都查不出「数字抄错了但抄得一致」。**唯一的防线是对着年报原文核**——
这一步没有捷径。
