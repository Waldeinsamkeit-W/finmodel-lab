# 部署说明

这个站是纯静态的：没有后端、没有构建步骤、不发任何外部请求。
所有路径都是相对路径，路由用的是 hash（`#/model/xxx`），
所以放在任何子路径下都能正常工作（`用户名.github.io/仓库名/` 也没问题）。

**部署到普通网页之后，「导出 Excel」和「导出备份」就能正常下载了**——
claude.ai 制品页下载不了，是因为它跑在带 sandbox 的 iframe 里，浏览器会拦掉页面自己发起的下载。
普通网页没有这个限制。

---

## 方案 A：GitHub Pages（推荐，免费、无需信用卡）

仓库已经初始化好，分支是 `main`，5 个提交，工作区干净。你只需要建远程仓库再推上去。

### 第 0 步：把提交署名改成你自己（可选但建议）

之前的提交是我用占位身份提交的。以后你自己提交时想署自己的名，先设一次：

```bash
git config --global user.name "你的名字" && git config --global user.email "你的邮箱@example.com"
```

### 第 1 步：建一个空仓库

去 <https://github.com/new>：

- 仓库名建议 `finmodel-lab`
- **必须选 Public** —— 免费账号的 Pages 只支持公开仓库（私有仓库要 Pro 方案）
- **不要**勾选 "Add a README file"、"Add .gitignore"、"Choose a license"，否则推的时候会冲突

### 第 2 步：推上去

在 `财务模型学习网站/` 目录里执行，把 `你的用户名` 换掉：

```bash
git remote add origin https://github.com/你的用户名/finmodel-lab.git && git push -u origin main
```

推的时候会让你输入用户名和密码。**密码那一栏不能填账号密码，要填 Personal Access Token**：

1. 去 <https://github.com/settings/tokens> → Generate new token (classic)
2. Note 随便写，Expiration 选个合适的
3. 勾上 **`repo`** 这一项（其他都不用勾）
4. 生成后把那串 `ghp_...` 复制下来，粘到密码栏

（Token 只显示一次，记得先存起来。粘贴时终端不会显示任何字符，这是正常的。）

### 第 3 步：打开 Pages

仓库页面 → **Settings** → 左侧 **Pages** → Source 选 **Deploy from a branch** →
分支选 `main`、目录选 `/ (root)` → **Save**。

等一两分钟（仓库的 Actions 页能看到部署进度），站点地址就是：

```
https://你的用户名.github.io/finmodel-lab/
```

### 第 4 步：验证

打开上面的地址，确认三件事：

1. 首页能列出 43 个模型（不是白屏）
2. 随便进一个模型，表格能正常显示
3. 点「导出 Excel」能真的下载到文件 —— 这是这次部署最主要的目的

### 以后更新

改完源文件后：

```bash
python3 build.py && git add -A && git commit -m "更新说明" && git push
```

推上去大约一分钟后线上就更新了。

### ⚠️ 关于 .nojekyll

仓库根目录有一个空文件 `.nojekyll`，**不要删**。

GitHub Pages 默认用 Jekyll 处理站点，而 Jekyll 会**忽略所有下划线开头的文件和目录**。
本站的 `data/_schema.js` 是 `index.html` 直接加载的，没有这个文件的话它会 404，整站白屏。
`.nojekyll` 的作用就是告诉 Pages「别跑 Jekyll，原样发布」。

## 方案 B：Vercel（推送即部署，速度更快）

先装 CLI 并登录（登录会打开浏览器，用你自己的账号授权）：

```bash
npm i -g vercel && vercel login
```

然后在这个目录里：

```bash
vercel --prod
```

它会问几个问题，全部回车用默认值即可（框架选 Other / 无需构建命令 / 输出目录留空）。
完成后会直接给你一个 `xxx.vercel.app` 的地址。

---

## 方案 C：不部署，直接发单文件

`dist/finmodel-lab.html` 是把 CSS 和全部脚本内联进去的单文件版（约 850 KB，零依赖）。
直接发给学生，双击用浏览器打开就能用，导出 Excel 也正常。适合不想维护服务器的情况。

重新生成：

```bash
python3 build.py
```

---

## 目录结构

```
index.html            入口，按顺序引入下面所有脚本
assets/
  engine.js           类 Excel 公式引擎（引用、区域、函数、求值）
  store.js            学习进度，存 localStorage
  diagnose.js         答错诊断（从「你的值 vs 参考值」推断错法）
  grid.js             表格交互（选区、编辑、填充柄）
  save.js             统一下载通道（宿主通道 → a[download] → 明确报错）
  xlsx.js             导出真正的 .xlsx（自己写的 ZIP + SheetML，导出公式而非结果）
  ui.js               路由与所有页面
  styles.css
data/                 模型与公司数据，每个文件是若干个模型
build.py              打包成单文件 HTML
_verify.js            开发期自检，见下
_sandbox-test.html    复现 claude.ai 制品的 sandbox 环境，用来验证下载通道
```

## 改完数据后请跑一遍自检

`_verify.js` 会对每个模型检查四类问题：公式报错、校验行不等于该等的值、
算出 NaN/Infinity、**公式指向空格或越界格**。最后一类最有用——
这种公式语法完全合法、也算得出数，只是把 0 当成了真实数据，靠肉眼很难发现。

用法：本地起个服务器，打开页面，在浏览器控制台里执行

```javascript
const s=document.createElement('script'); s.src='_verify.js'; document.head.appendChild(s);
// 加载完之后
verifyAll()          // 全部模型
verifyOne('模型id')   // 单个模型
```

`verifyAll()` 返回 `{total, passed, failed, cells, failures}`。
`failed` 应该只剩 `inovance-sotp` 一个——它有两处 0.16 / 0.06 的四舍五入残差，
来自年报分部数据本身的四舍五入，标签写的是「应接近 0」，属于已知且可接受。

本地起服务器：

```bash
python3 -m http.server 8777
```
