# 账号系统说明

学员用邮箱注册、登录后进度同步到云端；你能看到谁注册了、谁练到哪。

**没配置之前，整个站和以前一模一样**——不显示任何登录入口，进度只存本地。
`config.js` 里两个值填上了，账号功能才出现。所以你可以先把站发出去，
想做班级管理的时候再开。

---

## 一、装起来（大约 15 分钟）

### 第 1 步：建 Supabase 项目

去 <https://supabase.com> 注册，New project。地区选离学员近的（国内学员选 Singapore 或 Tokyo）。
数据库密码它会让你设一个，存好，但**平时用不到**——那是直连数据库用的，网站不需要。

### 第 2 步：建表和权限

后台左侧 → SQL Editor → New query → 把 `supabase-setup.sql` 整份粘进去 → Run。

这份 SQL 建两张表（`profiles` 学员资料、`progress` 学习进度），
装好行级安全策略，并加一个注册时自动建行的触发器。可以重复执行，不会覆盖数据。

### 第 3 步：把密钥填进 config.js

后台 → Project Settings → API，抄两个值填进 `config.js`：

```javascript
window.FML_CONFIG = {
  SUPABASE_URL: 'https://你的项目.supabase.co',
  SUPABASE_ANON_KEY: 'eyJ....'
};
```

> **anon key 可以公开**，它受行级安全策略约束，拿到它也只能读到自己那一行。
> 同一页上还有一个 **service_role key，那个是万能钥匙**——不要填进来、
> 不要提交到 git、不要发给任何人。

填完重新部署（`git add -A && git commit -m "开启账号" && git push`）。

### 第 4 步：把自己设成管理员

先用网站正常注册一个账号，然后回 SQL Editor 执行（换成你的邮箱）：

```sql
update public.profiles set is_admin = true where email = '你的邮箱@example.com';
```

刷新网站，侧栏底部就会出现「班级看板」。

### 第 5 步：决定要不要开邮箱验证

后台 → Authentication → Providers → Email → Confirm email。

- **开着**（默认）：注册后必须点验证邮件才能登录。更规范，能防止乱填邮箱。
  但免费版自带的邮件服务**有频率限制**（每小时几封），一个班同时注册会发不出来。
  真要开，去 Authentication → Emails → SMTP Settings 接自己的邮箱服务。
- **关掉**：注册完直接能用。小范围试用建议先这样。

---

## 二、你能看到什么

**谁注册了**：Supabase 后台 → Authentication → Users。有邮箱、注册时间、最后登录时间。

**谁练到哪**：网站侧栏的「班级看板」。每人一行：已开始几个模型、完成几个、
答对多少格、累计用时、最后活跃时间。

关于"谁在线"：做的是**最后活跃时间**（"刚刚 / 3 分钟前 / 昨天"），不是实时在线状态。
真正的实时在线要客户端不停发心跳，费流量也费额度，对判断学习情况没有额外价值。

---

## 三、几件需要知道的事

**登录是可选的。** 不登录照样能练，进度存本地。这是刻意的——发个链接就能试，
是这个站最大的传播优势，不该被登录页挡住。

**进度合并是按模型逐个来的。** 两台设备分别练了不同模型，登录后两边都保留。
同一个模型在两边都改过，取最后修改时间新的那份，并提示你合并了什么。
不会出现"一登录把本地全冲掉"的情况。

**本地永远是主。** 没网、Supabase 挂了、额度用完了，练习都照常，只是不同步。

**答案仍然在客户端。** 参考公式是打包进 JS 的，懂技术的人一行代码就能全打印出来。
账号系统解决的是"谁在练、练到哪"，**不解决考试防作弊**。真要考核，得另想办法。

**免费额度**：Supabase 免费版 500MB 数据库、5 万月活。每人的进度也就几十 KB，
一个班几十人用不掉零头。项目连续 7 天无请求会被暂停，进后台点一下就能恢复。

---

## 四、隐私说明（建议原样贴到「使用说明」页）

> 本站注册只收集**邮箱**和**昵称**。
> 邮箱用于登录、找回密码；昵称用于在班级看板上标识你。
> 你的做题进度会在登录后同步到服务器，只有你自己和课程管理员能看到。
> 我们不收集真实姓名、电话、住址，也不做任何第三方分析或广告追踪。
> 想删除账号和全部数据，发邮件给管理员即可。

如果以后要收更多信息（真实姓名之类），记得同步改这段，并想清楚为什么需要。
**能不收的就不收**——收了就要负保管责任。

---

## 五、出问题时

| 现象 | 多半是 |
|---|---|
| 没有登录入口 | `config.js` 两个值没填，或部署时没更新 |
| 注册说"这个邮箱已经注册过了" | 确实注册过了，直接登录；或去后台 Users 里删掉重来 |
| 注册后收不到验证邮件 | 免费版邮件频率限制。先关掉 Confirm email，或接自己的 SMTP |
| 登录后看不到「班级看板」 | 第 4 步的 SQL 没执行，或执行后没刷新页面 |
| 看板提示读不到数据 | 你的账号 `is_admin` 不是 true |
| 进度不同步 | 打开浏览器控制台看有没有报错；确认 SQL 里的策略都建成功了 |

想彻底关掉账号功能：把 `config.js` 两个值清空即可，站会退回纯本地模式，
已有的本地进度不受影响。
